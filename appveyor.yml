version: '{build}'
os: Windows Server 2012
install:
  - cd %APPVEYOR_BUILD_FOLDER%
  - git submodule update --init --recursive  
  - ps: |
      Add-Type -AssemblyName System.IO.Compression.FileSystem
      if (!(Test-Path -Path "C:\maven" )) {
        (new-object System.Net.WebClient).DownloadFile(
          'http://www.us.apache.org/dist/maven/maven-3/3.2.5/binaries/apache-maven-3.2.5-bin.zip',
          'C:\maven-bin.zip'
        )
        [System.IO.Compression.ZipFile]::ExtractToDirectory("C:\maven-bin.zip", "C:\maven")
      }
  - cmd: SET PATH=C:\maven\apache-maven-3.2.5\bin;%JAVA_HOME%\bin;%PATH%
  - cmd: SET MAVEN_OPTS=-XX:MaxPermSize=2g -Xmx4g
  - cmd: SET JAVA_OPTS=-XX:MaxPermSize=2g -Xmx4g
  - cmd: SET M2_HOME=C:\maven\apache-maven-3.2.5
  - cmd: for %%FILE in (*.wlk) do MKLINK "wollok-language\src\wollok\%%FILE" "org.uqbar.project.wollok.lib\src\wollok\%%FILE"
    )
build_script:
  - cd org.uqbar.project.wollok.releng/
  - mvn clean install -U -Darguments='-Dtycho.disableP2Mirrors=true' -Dtycho.disableP2Mirrors=true
test_script:
  - echo Already run :)
cache:
  - C:\maven\
  - C:\Users\appveyor\.m2
