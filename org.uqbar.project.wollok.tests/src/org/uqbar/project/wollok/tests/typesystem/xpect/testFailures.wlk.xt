/* XPECT_SETUP org.uqbar.project.wollok.tests.typesystem.xpect.TypeSystemXpectTestCase END_SETUP */

object pepita {
	const hola = "hola"
	
	// XPECT warnings --> "expected <<[]>> but found <<String>>" at ""2""
	const dos = "2"

	// XPECT warnings --> "expected <<Number>> but found <<String>>" at "hola"
	// method multiplicacion() = 2 * hola
	
	// XPECT warnings --> "expected <<Number>> but found <<Boolean>>" at "true"
	// method otraMultiplicacion() = 2 * true
	
	// TODO: Ver qué hacemos con esto
	/* XPECT warnings ---
		"String does not understand *( 2)" at "dos * 2"
		"expected <<Number>> but found <<String>>" at "dos"
	--- */
	method multiplicacion3() = dos * 2
	
}
