/* XPECT_SETUP org.uqbar.project.wollok.tests.xpect.WollokXPectTest END_SETUP */

object pepita {
}

object p {
    method run() {
        // XPECT warnings --> "Unnecessary if always evaluates to true !" at "true"
        if (true)
            throw new Exception("asd")
    }
}