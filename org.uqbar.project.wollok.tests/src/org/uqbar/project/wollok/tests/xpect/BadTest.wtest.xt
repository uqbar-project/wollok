/* XPECT_SETUP org.uqbar.project.wollok.tests.xpect.WollokXPectTest END_SETUP */

describe "group of tests" {
	
	// XPECT errors ---> "Fixture must not be empty" at "fixture {}"
	fixture {}
	
	// XPECT errors ---> "Tests must not be empty " at "test "a test" { 		 	}"
	test "a test" {
		
	}
}
