import zuper.zuper.*

program zuper {
	
//	val x = wollok.lib.assert
	
	val pepona = new simpleInheritance.Golondrina()
	pepona.volar(50)
	
	assert.that(pepona.energia() == 0) // Gastó el doble de energia
	
	val o = object {
		method valak() {
			// super() // FAIL OK !
		}
	}
	
}