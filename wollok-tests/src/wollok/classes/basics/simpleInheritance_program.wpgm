import simpleInheritance.*

program simpleInheritance {
	
	const pepita = new Golondrina()
	pepita.volar(50)

	assert.equals(50, pepita.energia())

	pepita.setEnergia(23)

	assert.equals(23, pepita.energia()) // Mando mensaje en superclase
	
}