
object alpiste {
	method energia() = 100
}
object maiz {
	method energia() {
		return 200
	}
}

test "Energia del Alpiste es 100" { 
	assert.equals(100, alpiste.energia())
}

test "Energia del Maiz es 200" { 
	assert.equals(200, maiz.energia())
}